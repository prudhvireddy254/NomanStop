# Copy to .env in the project root for local Expo development
# cp env.example .env

# Your computer's LAN IP when testing on a physical phone (e.g. 192.168.1.42)
# Leave unset when using iOS Simulator / Android Emulator — the app auto-detects the host.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
