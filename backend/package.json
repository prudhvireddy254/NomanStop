{
  "name": "nomanstop-backend",
  "private": true,
  "description": "Helper scripts to run backend services — no dependencies here",
  "scripts": {
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "install:all": "npm install --prefix auth-service && npm install --prefix api-gateway",
    "auth:dev": "npm run start:dev --prefix auth-service",
    "gateway:dev": "npm run start:dev --prefix api-gateway",
    "auth:build": "npm run build --prefix auth-service",
    "gateway:build": "npm run build --prefix api-gateway",
    "db:migrate": "npm exec --prefix auth-service prisma migrate deploy",
    "db:seed": "npm exec --prefix auth-service prisma db seed"
  }
}
